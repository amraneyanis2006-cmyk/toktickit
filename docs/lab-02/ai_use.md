# AI Usage Log (`ai_use.md`)

| Prompt # / Stage | Objective / Key Prompt | AI Output & Assistance | Human Oversight & Reflection |
| :--- | :--- | :--- | :--- |
| **01–09. Core Implementation (Requester Selection, Create Ticket, My Tickets, Ticket Detail, Attachments backend/frontend, ownership scoping)** | Get a general line of direction and help structuring the project across the early Lab 2 issues. | AI was used to help structure the project, suggest an overall approach for each feature area, assist with debugging, and sanity-check major implementation steps before moving on. | Every suggestion was reviewed and adapted to the actual codebase before being committed; final correctness for each Issue was verified against `api-spec.md` / `ui-spec.md` / `tests.md` and via the automated test suites (Vitest, Supertest), not taken on trust from the AI output alone. |
| **10. Ownership Audit** | Review all Requester-scoped endpoints against BR-08/BR-22/BR-27 to confirm no cross-Requester data leaks exist. | AI helped walk through each of the 6 scoped endpoints systematically and cross-check the 404-vs-403 behavior on cross-Requester lookups against the "identical response either way" rule in `api-spec.md` §6. | Findings were manually verified against the running API before being written up in the PR description; no code changes were needed since the audit confirmed existing scoping was already correct. |
| **11. Responsive/Visual QA Suite** | Build a Playwright suite covering RESP-01/02/03 and VIS-01/02, and capture the full screenshot archive per `ui-spec.md` §10. | AI helped scaffold the Playwright specs and screenshot capture flow, and helped diagnose the Ticket Detail mobile Download button tap-target issue (measured at 31px, below the 44px WCAG 2.5.5 minimum). | The accessibility bug was manually confirmed with a real measurement before the fix; the CSS fix (mobile-only `min-height` rule in `zen-green.css`) and the resulting `≥44` assertion were reviewed and verified against 2 consecutive Playwright runs (29/29 green) before merge. |
| **12. End-to-End Test Suite** | Finalize and commit/push the 5 remaining E2E specs (create-ticket-flow, cross-requester-isolation, attachment-lifecycle, my-tickets-search-filter, inactive-requester-exclusion), then open the PR into `lab2-staging`. | AI assisted with drafting the commit message and PR description, and with working through Siméon's review questions on PR #12 (Playwright worker/idempotency behavior, and confirming E2E-02 validates isolation at the API level, not just the UI). | All git commands (`add`, `commit`, `push`) were run and verified by the human at each step (`git status` checked before and after); PR review responses were written and posted by the human after confirming the technical claims (e.g. reading `playwright.config.ts` for the `workers: 1` setting) against the real repo, not assumed. |
| **13. Lab 2 Release Prep** | Finalize the release: add a Lab 2 section to `README.md`, write `docs/lab-02/reviewer.md` and this `ai_use.md` file with real evidence, and verify the Definition of Done ahead of the `lab2-staging` → `main` PR. | AI drafted the README Lab 2 section and the `reviewer.md`/`ai_use.md` templates, and helped investigate a `server/.env` file that had been committed early in the sprint (commit `6b5cfbb6`) and later removed from tracking (`1aa649d7`) — confirming via `git show` that the committed content was only Prisma's default local-dev boilerplate, not a real secret. | Every claim in these docs was checked against actual command output (`git log`, `git show`, `git ls-files`, `ls`) before being written down; AI-drafted review-comment summaries were replaced with the real GitHub PR comments once the human copied them over, rather than being left as plausible-sounding fabrications. |